"use strict";
(function () {
    var $window = $(window);
    var $lpage = $('.lpage');

    var pages = [
        [null, 'index', null],
        ['dev', 'security', 'hobby'],
        [null, 'about', null]
    ];
    var currentPage = [0, 1];
    // bind arrow keys

    $('nav > .up').click(function (e) {
        e.preventDefault();

    })
})();